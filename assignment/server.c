#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>
int id;

int main(int argc, char *argv[])
{
    id = getpid();
    c1_id = //;
    c2_id = //;

    printf("Please wait until server is ready ...");
    // 서버 클라이언트 연결

    printf(" Done\n");

    printf("Server %d is rady for play!\n", id);
    printf("Client %d hi with slot 0");
    printf("Client %d hi with slot 0");

    pritnf()

    while(1){
        int client1 = getClient(c1_id);
        printf("[%d] %d", c1_id, client1);

        int client2 = getClient(c2_id);
        printf("[%d] %d", c2_id, client2);
    }

    printf("Player %d lose", cl_id);
    printf("Client %d", cl_id);
    printf("One of client is disconnected");

    return 0;
}